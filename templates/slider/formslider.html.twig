{# 
  $builder
            ->add('titre')
            ->add('description_courte')
            ->add('description_longue')
            ->add('photo')
            ->add('prix_journalier')
            ->add('date_enregistrement')
 #}

 {% extends 'base.html.twig' %}

{% block title %}formulaire slider{% endblock %}

{% block body %}
    <h1>formulaire slider</h1>
{# propriete formulaire
  $builder
            ->add('titre')
            ->add('description_courte')
            ->add('description_longue')
            ->add('photo')
            ->add('prix_journalier')
            ->add('date_enregistrement') #}
{# {{ form(form.Chambre)}} #}

{# exemple de formulaire
{% extends "base.html.twig" %}

{% block title %}ajout voiture{% endblock %}

{% block body %}
    <h1 class="text-center my-4">Ajout Voiture</h1>
    <div class="container">
        {{ form_start(formVoiture)}}
            {{ form_row(formVoiture.marque)}}
            {{ form_row(formVoiture.modele)}}
            {{ form_row(formVoiture.prix)}}
            {{ form_row(formVoiture.description)}}
            <button class="btn btn-primary">Ajouter</button>
        {{ form_end(formVoiture)}}
    </div>
    <p>toto</p>
{% endblock %}
 #}
 

  <h1 class="text-center my-4">Ajout slider</h1>
    <div class="container">
        {{ form_start(formslider)}}
        
            {{ form_row(formslider.photo)}}
            {{ form_row(formslider.ordre)}}
           
            {{ form_row(formslider.date_enregistrement)}}
            
            <button class="btn btn-primary">Ajouter</button>
        {{ form_end(formslider)}}
    </div>
    {# <p>toto</p> #}

{% endblock %}